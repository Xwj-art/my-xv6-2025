### Implement copy-on-write fork

1. 明确需求。创建子进程时，清除PTE_W，子进程与父进程映射到同一页。写入数据时，若可写，复制为新页，否则错误。使用引用计数来约定释放页的时机。发生错误或内存不够，杀死进程。
2. 先更改kalloc能够使用引用计数。我该怎么来判定是要分配内存还是增加引用计数呢？如果是写入数据，并且可写，则分配内存，如果是fork，则增加引用计数，这个逻辑应该是在外面判断，传参数表示情形，为了不改变其他函数的接口，我们分析出这个分配内存和增加引用的逻辑只与父子进程有关函数相关，我们可以再实现一个函数cowalloc来实现以上功能。
3. 理一下cow的使用场景。当页面只有读权限时，不需要cow，当页面有写权限时，若有子进程，需要pte_c=1，pte_w为0，无子进程，pte_w=1，pte_c=0。我们只需要关注写的场景即可，pte_w是一个标志，告诉我们写时错误是否需要分配页面。若vmfault传来的参数中read=1，读权限错误，必错；写权限错误需要我们判断是否pte_c为1，即原来可以写。
4. vmfault中，是要判断是否是写，如果是写，并且pte_c为1，则需要分配内存。如果是读，

5. 捋一下。大概是这些地方要改，一个usertrap的处理，如果是r_scause()=15并且pte_c为1，需要复制内存；kalloc.c里添加引用计数机制来实现cow；vm.c中，一个vmfault，一个copyout，都是需要写入数据，判断pte_c=1，复制内存；uvmcopy，fork时增加引用计数。

6. 捋一下。首先cow不会页缺失，因为已经cv完了，所以读不会错误，错了就是杀死，必定是权限不允许，写会错误，需要分配页。

7. 已经过了cowtest测试用例，只剩下usertest的俩了这时候，感觉对哪些东西panic然后在kerneltrap处理，内核崩了；哪些东西在usertrap处理，杀死进程内核继续运转有了理解了，之前确实没有关注过这个trap分为user和kernel的，偷懒和为了方便调试直接在实现的功能里用了大量的panic，后面都找出来改了。而usertest里的话，是遇到了lazy_alloc测试用例没有过，所以这个实验是支持这两种分配方法的，cow和lazy_alloc，那么这时候我已经实现了cow，还如何在usertrap中使用懒页分配？usertrap处理这里，我该如何辨别是权限位出错还是缺页呢？这两个最大的区别应该是有无分配物理页。如果访问的地址在分配的范围里，但是访问不成功，那么就是缺页，否则就是有页但是还是访问错误，检查一下是不是cow页。
