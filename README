## MMAP

这个实验要实现的是MMAP和MUNMAP，首先要大致定义好vma结构体，并明确各个部分分别在哪里按照什么顺序实现。sys_mmap和sys_munmap在sysfile.c中获取user中传递来的参数，再调用mmap和munmap两个辅助函数，mmap的作用是将file映射到虚拟内存空间，返回这段虚拟内存空间的地址，将对文件的操作转换为对内存空间的操作，若修改后需要返回则将内容写回。需要做到选取合适的虚拟地址范围，保证不与p->sz部分重合。一种方法是直接跟着sz后面来分配地址；另外一种方法是从后面向前分配空间。
这样就可以把map部分先实现，然后就是把缺页部分的逻辑写了，缺页了如果是在map映射的内存范围内，那么需要分配实际物理页。
再就是unmap部分了。这个部分是要将map的内存部分写入数据到file里，再减少文件的引用计数。首先明确回收的内存的话应该是hint提到的开头、结尾或者全部，找到范围后可能要判断是否要回收，因为有的内存部分可能都没有进行物理页的映射，如果要写回的话那就是：读物理页数据时缺页并分配物理页，然后将没有数据的物理页写回文件中，再将物理页回收。这样反而麻烦，所以会有一步判断是否分配物理页，未分配或者map_private则这部分不用写回。明确了写回的部分后，就是要将这部分写回到文件中。
调用一次unmap是否需要回收多个vma呢？不需要，因为hint明确了回收范围。
有一个叫filewrite的函数，将虚拟地址addr处开始的n个字节数据写入file里。而要判断这一页是否分配出去了，是用的walkaddr，这个只能判断这一页是否分配了出去，但是每次unmap的虚拟内存空间都是首、尾、全部三段，不会有不间断的内存空间，而是已经分配出去的写了数据的连续的空间，则只需要判断addr处是否开始了物理地址。若这个vma所映射的区域全都unmap了，则需要fileclose，这里差点忘了更新ummap部分区域后vma的addr和len了。

在基本实现map和unmap后，开始写fork和exit部分。exit的话将所有的vma都回收了。

在测试的时候发现权限错了，又看了一下hint是说的PROT_write+map_private也只是写完以后不写回就好了，而不是直接判定这种情况是错的。看了一下test文件里的情况，其实是忽视了file的读写权限。首先是可以文件不可读，那么根本就读不了文件中内容；其次是在可以修改文件的情况下，文件是否可写，map写文件的话是prot_write && map_shared，这样才能写内存并写回文件。
然后是卡在了怎么将内存对应的数据写到文件中，这分别需要内存区域和文件的基准值和偏移值。而现有的filewrite只能使用file维护的off作为固定的偏移值，有一些操作需要指定偏移值，比如需要从起始处开始，或从第二页开始数据访问。如何写出文件的offset呢？因为我vma的addr是会更新的，每当前面一段被释放掉都会更新addr，指定的这个off是相对于起始addr的偏移量，我可以维护一个偏移量表示当前基准地址相对于起始地址的偏移量。但是如果我不释放掉前面的位置也要能够指定在哪个位置上写呢？所以那个munmap部分不能够更新vma的addr才对吧。但是实在不知道惰页分配的情况下该怎么不改变起始addr将前一部分页面释放掉。
最后一点儿是卡在exit这儿，多线程调试实在是麻烦，通过打印文件增长进行调试发现并不是按照页数增长而是按照块大小增长，从writei、readi到filewriteoff最后到kexit才确定是子进程退出时写入数据量更新错误。
