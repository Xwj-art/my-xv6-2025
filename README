## Lab2

### gdb练习

在mac写的，所以用的lldb，这个练习反而花时间挺久的，答案写answers-syscall.txt里了。

### sandbox

1. 其实实验比较简单，如果跟着前面的gdb跑的时候看了proc.c和syscall.h里的系统调用表，对流程是比较清楚的。比如最开始gdb练习中问的a7里的15是什么意思？其实就是SYS_open，os通过这个a7通过函数指针来选取对应的系统调用。
2. hints写的比较详细了，主要是知道要写哪些东西，比如说系统调用服务表加上一个SYS_interpose=22这一项，以及内核与用户区数据的数据是怎么传递的，这个要看看其他系统调用怎么传的参数，在syscall函数里对于proc里的mask判定的逻辑其实比较简单，主要是理解整个系统调用的流程以及代码是怎么实现的。
3. 遇到的困难应该就是一下子没想出来怎么去拿到grep或者cat之类命令的那个文件名，在那里纠结哪个参数是文件名，但是后面想起来hint里其实已经说的很清楚了，是系统调用的禁止，所以我只要禁用掉open这个系统调用即可，而open这个系统调用的文件名其实就是第一个参数，所以使用argstr拿到第一个参数就行。

### attack

1. 这个实验要干嘛其实一开始没怎么看懂，大概捋了一下，就是上一个进程在内存中写完数据，但是运行完未释放和清空，仍然有数据残余，所以我要在attack.c实现一个函数，来申请空间，然后扫描空间里的数据，看看有没有能打印的。
2. 这个实验对我来说第一个难点应该就是不知道要做什么，这个流程很陌生，像前几个实验是知道步骤或者对数据是从哪个变量到哪个地方流通比较熟悉，但是这个实验secret以后就是分配页了，页中数据对我完全透明，所以一开始会摸不着头脑。
3. 知道数据是分配在内存中，sbrk系统调用则是来请求分配内存，那么其实我们上述2其实也不用管，可以直接写，毕竟很明确的就是一直申请内存空间并扫描，找到所要找的数据打印即可，所以即使透明也无需慌。
4. 第二个难点应该就是一开始没想到怎么判定是否是我要找的密钥了。因为垃圾数据太多，一开始用的测试用例很长，所以判定的只打印长度>5的字符串，因此help部分并未打印出来，所以想了很久。直到后面慢慢开始将len改为>2跑了几次才发现规律，原来secret在help后面。
5. 跑的时候会发现有俩help，一个后面是字符串"null"一个后面是serect，我想这就是可能要多次attack的原因，因为help来判定secret的位置的话，会因为常量有一个help和data有一个help而导致可能第一次找不到。

```c
strcpy(data, "This may help.");
strcpy(data + 16, argv[1]);
```
