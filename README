### Large files

1. 分析需求，就是再多加一个指向块，块内是inode块的索引，然后这些索引又指向一个块，这个块内是inode，双重间接索引，应该是让我们熟悉一些内存中文件和磁盘中文件的数据结构，以及一个数据从磁盘到内存的过程。所以我们不难看到，在二级索引指向一个块的时候，其实中间还会有一个间接去找两个块的过程，这两个块内会有inode块的索引和inode，找到这个inode之后去指向文件。

### Symbolic links

1. 添加软链接文件这一类型，实现的函数功能是给定一个target和path，path上创建一个软链接文件，内容指向目标文件。先学习一下link/unlink里的函数，namei、日志事物以及加锁对文件系统的处理，还有iunlockput，在错误处理或者正常结束时，若不再对当前inode进行操作，需要调用，用于通知不再使用这个inode，当引用计数为0时，释放掉inode，引用计数：指向inode的指针数目；dirlookup，查找有无节点。调用对inode节点操作对函数会对inode对引用计数++
2. 接下来写sys_open，主要是遇到symlink时分成直接当成文件取里面的数据，以及当成软连接递归找到最终指向的数据文件两种情况，其中，递归层数在hint种已经说明了不会超过10。将这个递归查找的逻辑抽象成follow_symlink函数。

这俩实验抽象的比较高，一开始对inode操作的begin_op和end_op是什么意思都没有弄清楚，以及iput和iunlockput也要区分。
