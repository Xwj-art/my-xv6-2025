### Memory allocator

这个实验得先把xv6
book看了才知道要干啥。在book里是有讲到为何维护一个全局的freelist会效率低下的。这样的设计在多cpu并行的时候，各个进程争用kalloc和kfree的时候，会去抢同一把锁，但是如果设计每个CPU各有一部分RAM，而且还能在自己这部分内存用完以后去steal其他进程的RAM，那这样的话kalloc和kfree的时候就不再需要去上锁了，因为CPU是在自己的freelist上分配内存，不会出现race的情况。但是由于steal，需要可以在自己内存用光是检测到其他CPU内存是否足够多的空余内存，并steal下来进行使用。其实这样也就相当于是将一个CPU的freelist来分配个多个CPU，会需要lock。

所以实验第一步是先将freelist分配好。给出的提示是freelist还是像原来一样分配所有的内存，为什么是分配所有内存而不是各个CPU平分内存？因为若CPU的freelist不足的话，则可以steal其他CPU的freelist。那么我们只需要初始时只有一个CPU的freelist为所有内存，其他CPU不够自己会去steal。


### Read-write lock

这个是一个读写者且写优先的实现，需要有一个mutex来在writer排上队后，将比writer后来的reader上锁，免得就算writer先来排队了，但是永远都有reader来，不释放mutex。
我们需要考虑这个读写锁该如何实现。读写锁中应该是要有两个锁来分别进行reader和writer的同步互斥管理的。需要一个计数器来记录下当前有多少个读进程，若有读进程，则writer_acquire需要排队，reader_acquire若在writer前面则能进来并且不需要再次上锁，若不能则需要等writer进去操作完再进来；若无读进程，则writer_acquire直接进去上锁，reader_acquire排队，等里面没有writer的时候进去上锁。
所以我们要考虑怎么实现这个写优先问题，为这个写优先加的一把用于实现同步的锁该如何用呢？应该是每进去一个都得先要拿到这个mutex，reader进去了就能直接放了，因为可以多个reader进去做事儿；但是如果writer进去了就得把事情做完才能放，而在此期间由于mutex在writer这儿，所以其他进程都不能进来，这就实现了一种排队。

多个reader是否可行呢？可行，因为只acquire-release了一次，不会出错。那么算上writer呢？算上writer的话，会有个while相当于自旋锁，reader需要排队等待前面的writer先做完释放这个自旋锁。writer是否可以拿到锁呢？这里需要等到reader不持有锁才可以，怎么才能不持有锁？应该是count为0的时候。

需要注意acquire的条件是什么。

其实一开始弄错了，这个rwspinlock其实本身就是一把锁，用来实现读写者进程机制的。而怎么实现这个机制呢？其实是用的循环（自旋）来实现的，而不是一开始以为的在里面弄锁。一开始弄锁是因为给了一个锁，以为只是单纯的对这个锁进行封装，只要拿到这个锁就可以对时钟进行访问之类的。但是实际上这个rwspinlock本身就是锁，应该是拿到这个rwspinlock就可以对时钟进行访问了，而这个rwspinlock是对tick的锁，针对reader和writer进程会有不同的处理机制，所以里面会设置一些不同的成员变量，而多个进程在调用函数时会操作这些变量，这个时候我们才用到了这个锁，这个锁实际上是用来实现内部成员变量的互斥访问的，这是这个锁的作用。
第一个难点应该是成员变量怎么设置出来的。一开始只是简单的设置了一个flag来代表是否有writer等待，但是后面发现单纯的设置一个flag的话会造成多个writer等待但是在第一个writer访问完以后flag为无writer等待的局面。遂将flag换成了writer等待的数量。然后就是一开始判断reader、writer等待的条件有些差错，光想着reader要是有reader在里面就不用acquire，若没有reader那就判断一下writer是不是在里面再acquire；忽略了writer需要判断其他writer是否在里面，只有无进程拿锁的时候writer才能acquire。然后跟着提示就设置了writer变量用于判断是否是writer当前持有锁，而reader的计数器则是可以用于reader是否当前持锁。

最后usertests会有个pause的过不去，sleep对tickslock但是sleep接受的类型是spinlock而不是rwspinlock，需要改一下sys_pause()。
